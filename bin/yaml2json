#!/usr/bin/env sh
# vi: :set ft=sh
python -c """
import sys
import yaml
import json

docs = yaml.load_all(sys.stdin)
json_docs = list()
for doc in docs:
    json_docs.append(doc)

print(json.dumps(json_docs))
""" | jq  .
